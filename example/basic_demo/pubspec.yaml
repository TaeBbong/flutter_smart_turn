name: basic_demo
description: Basic demo of flutter_smart_turn — mic → turn detection → visual display.
publish_to: 'none'

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_smart_turn:
    path: ../../
  record: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
