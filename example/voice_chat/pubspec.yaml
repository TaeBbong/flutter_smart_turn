name: voice_chat
description: End-to-end voice chat demo with ASR + LLM + TTS + flutter_smart_turn.
publish_to: 'none'

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_smart_turn:
    path: ../../
  record: ^6.2.0
  http: ^1.6.0
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
